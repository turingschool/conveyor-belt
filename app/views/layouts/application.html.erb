<!DOCTYPE html>
<html>
  <head>
    <title>ConveyorBelt</title>
    <%= csrf_meta_tags %>

    <link href="https://unpkg.com/basscss@7.1.1/css/basscss.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,700,800" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <div class="p2 ml4 left logo">
        <strong><span class="icon">//</span> conveyor belt</strong>
      </div>
      <div class="p2 mr4 right">
        <% if current_user %>
          Signed in as <%= current_user.name %> |
          <%= link_to "Sign out", signout_path %> |
          <% if current_user.admin? %>
            <%= link_to "Dashboard", dashboard_path %>
          <% end %>
        <% else %>
          <%= link_to "Sign in with Github", "/auth/github", class: "login-btn btn btn-primary" %>
        <% end %>
      </div>
    </nav>

    <main class="px4">
      <% flash.each do |key, msg| %>
        <%= content_tag :div, raw(msg), class: "#{key} col col-12 center p1 mt2 mb3" %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
